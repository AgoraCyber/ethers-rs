[workspace]
members = [
    "providers","utils",
    "ethers","macros",
    "parity/ethabi",
    "signer",
    "wallet",
    "types",
    # "parity/ethereum-types",
    # "parity/fixed-hash",
    # "parity/primitive-types",
    # "parity/uint"
]