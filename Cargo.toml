[workspace]
members = [
    "providers","utils",
    "ethers","macros",
    "parity/ethabi",
    "signer"
    # "parity/ethereum-types",
    # "parity/fixed-hash",
    # "parity/primitive-types",
    # "parity/uint"
]